"""SQLite database operations for activity tracking."""

# TODO: v0.2.0 - Implement database module
# - init_db(db_path): create activity_logs table + indexes, enable WAL mode
# - insert_activity_log(conn, sample): insert one sampling record
# - query_date_stats(db_path, date): return totals + by_category + by_process
# - query_entertainment_trend(db_path, start, end): multi-day video trend
